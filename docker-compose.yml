services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: plm_workbench
      POSTGRES_USER: plm
      POSTGRES_PASSWORD: plm
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
